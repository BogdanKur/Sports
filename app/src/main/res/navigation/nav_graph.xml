<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/exerciseListFragment">
    <fragment
        android:id="@+id/exerciseListFragment"
        tools:layout="@layout/fragment_exercise_list"
        android:name="com.example.sportsexercises.ExerciseListFragment">
        <action
            android:id="@+id/action_exerciseListFragment_to_opinionExerciseFragment"
            app:destination="@id/opinionExerciseFragment"/>
    </fragment>
    <fragment
        android:id="@+id/opinionExerciseFragment"
        tools:layout="@layout/fragment_opinion_exercise"
        android:name="com.example.sportsexercises.OpinionExerciseFragment">

    </fragment>
</navigation>